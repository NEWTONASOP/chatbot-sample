{
  "name": "travel-ai-chatbot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist --compatibility-date=2024-01-01",
    "deploy": "npm run build && wrangler pages deploy dist"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.36.4",
    "vite": "^6.1.0",
    "wrangler": "^4.90.1"
  }
}
