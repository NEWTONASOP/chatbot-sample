{
  "name": "travel-ai-chatbot",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler pages deploy dist --project-name=travel-ai-chatbot"
  },
  "devDependencies": {
    "vite": "^6.1.0",
    "wrangler": "^4.90.1"
  }
}
